name: Build Windows Installer

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: # Memungkinkan Anda menjalankan build secara manual dari web GitHub

jobs:
  build-windows:
    name: Build MSI (Windows)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build MSI Installer
        run: |
          mkdir -p composeApp/src/jvmMain/resources
          echo '${{ secrets.SERVICE_ACCOUNT_JSON }}' > composeApp/src/jvmMain/resources/service-account.json
          ./gradlew :composeApp:packageMsi


      - name: Upload Windows MSI
        uses: actions/upload-artifact@v4
        with:
          name: ScannerKKP-Windows-Installer
          path: composeApp/build/compose/binaries/main/msi/*.msi
